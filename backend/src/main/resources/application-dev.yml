spring:
  datasource:
    url: jdbc:h2:file:./.betaup-dev/betaup;MODE=MySQL;AUTO_SERVER=TRUE;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:db/h2/dev-compat.sql
  h2:
    console:
      enabled: true
      path: /h2-console
